# Supabase project credentials — used ONLY to submit leads from the "Apply for
# Service" form. This is the same Supabase project FNDS (the separate admin
# app) reads from. Find these in Supabase: Project Settings -> API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
